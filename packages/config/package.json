{
  "name": "@local-service-marketplace/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript configuration for the Local Service Marketplace monorepo",
  "files": [
    "tsconfig.base.json",
    "tsconfig.next.json",
    "tsconfig.nest.json"
  ],
  "scripts": {
    "lint": "echo \"No lint for config package\"",
    "typecheck": "echo \"No typecheck for config package\"",
    "test": "echo \"No tests for config package\""
  }
}
